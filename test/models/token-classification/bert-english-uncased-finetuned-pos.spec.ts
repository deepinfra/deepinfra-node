import {BertEnglishUncasedFinetunedPos} from '@/index';

describe('BertEnglishUncasedFinetunedPos', () => {
  it('should be exported', () => {
    expect(BertEnglishUncasedFinetunedPos).toBeDefined();
  });
});
